# API

## Públicas

GET /materias GET /materias/{id} GET /unidad/{id} GET /contenido/{id}

## Sesiones

POST /sesiones GET /sesion/{codigo} POST /evaluacion/enviar

## Administración

CRUD materias CRUD unidades CRUD preguntas CRUD contenido CRUD grupos
CRUD alumnos Reportes
